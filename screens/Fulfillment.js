import {Button, View} from 'react-native';

function FulfillmentScreen({navigation}) {
  return (
    <View
      style={{flex: 1, alignItems: 'center', justifyContent: 'center'}}></View>
  );
}

export default FulfillmentScreen;
